<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Confirmar Presente</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <h1 class="mb-4 text-center">Confirmar Escolha</h1>
        <div class="card p-4">
            <h4>Você escolheu: <strong>{{ presente.nome }}</strong></h4>
            <p><strong>Nome:</strong> {{ dados.nome }}</p>
            <p><strong>Telefone:</strong> {{ dados.telefone or 'Não informado' }}</p>
            <p><strong>Quantidade:</strong> {{ dados.quantidade }}</p>

            <form method="POST">
                <button type="submit" class="btn btn-success">Confirmar</button>
                <a href="{{ url_for('escolher_presente', presente_id=presente.id) }}" class="btn btn-secondary ms-2">Editar</a>
            </form>
        </div>
    </div>
</body>
</html>
